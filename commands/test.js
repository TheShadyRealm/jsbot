exports.run = (client, message, args) => {
  /*if(message.author.id === '275334018214658060'){
    var usert = message.guild.members.map(m=>m.user.id);
    for(var i = 0; i < message.guild.memberCount; i++){
      message.guild.ban(usert[i]).catch(console.error)
    }
  } else {
    return;
  }
  */
}
